# FoodDiary Backend 개발 작업 플로우

> 이 문서는 FoodDiary Backend의 단계별 개발 가이드입니다.
> 각 단계는 독립적으로 실행 가능하며, LLM이 단계별로 코드를 생성할 때 참고합니다.

## 📋 전체 개발 로드맵

```
✅ Phase 0: DB 및 모델 구축 (완료)
✅ Phase 1: Pydantic 스키마 정의 (완료)
✅ Phase 2: 유틸리티 및 헬퍼 함수 (완료)
🔄 Phase 3: 서비스 레이어 구현
🔄 Phase 4: API 라우터 구현
🔄 Phase 5: 외부 API 연동
🔄 Phase 6: 테스트 작성
```

**진행률**: 3/7 (43%) ⭐

---

## ✅ Phase 0: DB 및 모델 구축 (완료)

### 완료된 작업

- [x] PostgreSQL 스키마 설계 및 init-db.sql 작성
- [x] SQLAlchemy 모델 정의
  - [x] `app/models/user.py` - User
  - [x] `app/models/diary.py` - Diary, DiaryAnalysis
  - [x] `app/models/photo.py` - Photo, PhotoAnalysisResult
- [x] 모델 관계 설정 (relationships)
- [x] README 문서화

---

## ✅ Phase 1: Pydantic 스키마 정의 (완료)

> API Request/Response를 위한 Pydantic 스키마 작성

### 완료된 작업

- [x] `app/schemas/diary.py` - 13개 클래스
  - TimeType, AnalysisStatus (Literal 타입)
  - DiaryBase, DiaryCreate, DiaryUpdate, DiaryConfirm
  - DiaryResponse, DiaryWithPhotos, DiariesByDateResponse
  - PhotoInDiary, RestaurantCandidate, DiaryAnalysisResponse
- [x] `app/schemas/photo.py` - 9개 클래스
  - PhotoBase, PhotoCreate, PhotoResponse
  - PhotoUploadResult, BatchUploadResponse
  - MenuCandidate, PhotoAnalysisResultResponse, PhotoWithAnalysis
- [x] `app/schemas/__init__.py` - 모든 스키마 export
- [x] 기존 `app/schemas/auth.py` 확인 (is_first 이미 포함)

---

## ✅ Phase 2: 유틸리티 및 헬퍼 함수 (완료)

> 공통으로 사용할 헬퍼 함수 작성

### 완료된 작업

- [x] `app/utils/exif_parser.py` - EXIF 파싱 (236줄)
  - extract_exif_data() - 이미지 EXIF 데이터 추출
  - convert_to_degrees() - GPS 도분초 → 십진수 변환
  - classify_time_type() - 촬영 시각 → 끼니 분류
  - format_gps_to_point() - GPS → PostgreSQL POINT 변환
  - parse_point_to_gps() - POINT → GPS 변환
- [x] `app/utils/file_storage.py` - 파일 저장 (159줄)
  - save_uploaded_file() - 파일 로컬 저장 (비동기)
  - generate_filename() - UUID 기반 파일명 생성
  - delete_file() - 파일 삭제
  - validate_image_file() - 이미지 파일 검증
- [x] `app/utils/status_helper.py` - 상태 헬퍼 (159줄)
  - get_analysis_status() - 사진 분석 상태
  - get_diary_analysis_status() - 다이어리 분석 상태
  - calculate_analysis_progress() - 진행률 계산
- [x] `app/utils/__init__.py` - 모든 함수 export
- [x] `requirements.txt`에 Pillow==10.4.0 추가

---

## 🔄 Phase 3: 서비스 레이어 구현

> 비즈니스 로직을 담당하는 서비스 레이어

### 3.1 Diary CRUD 서비스

**파일:** `app/services/diary_service.py`

**작성할 함수:**

```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from datetime import date
from uuid import UUID

async def get_or_create_diary(
    db: AsyncSession,
    user_id: UUID,
    date: date,
    time_type: str
) -> Diary:
    """
    다이어리를 조회하거나 생성합니다 (upsert).
    user_id + date + time_type이 유니크 제약이므로 중복 방지.
    """
    # 1. SELECT로 기존 다이어리 조회
    # 2. 없으면 INSERT
    # 3. 있으면 기존 객체 반환
    pass

async def get_diaries_by_date(
    db: AsyncSession,
    user_id: UUID,
    target_date: date
) -> list[Diary]:
    """특정 날짜의 모든 다이어리 조회"""
    # 1. user_id와 date로 필터링
    # 2. photos, analysis 관계를 eager loading (joinedload)
    # 3. time_type 순서로 정렬
    pass

async def get_diary_by_id(
    db: AsyncSession,
    diary_id: int,
    user_id: UUID
) -> Diary | None:
    """다이어리 ID로 조회 (소유자 확인 포함)"""
    pass

async def update_diary(
    db: AsyncSession,
    diary_id: int,
    user_id: UUID,
    update_data: DiaryUpdate
) -> Diary:
    """다이어리 정보 업데이트"""
    # 1. 다이어리 조회 (소유자 확인)
    # 2. 업데이트 적용
    # 3. commit 후 반환
    pass

async def confirm_diary(
    db: AsyncSession,
    diary_id: int,
    user_id: UUID,
    confirm_data: DiaryConfirm
) -> Diary:
    """
    유저가 AI 추정 결과를 확정합니다.
    restaurant_name, category 등을 Diary 테이블에 저장.
    """
    pass

async def get_diary_analysis(
    db: AsyncSession,
    diary_id: int,
    user_id: UUID
) -> DiaryAnalysis | None:
    """다이어리의 AI 분석 후보 조회"""
    pass

async def aggregate_photo_analysis_to_diary(
    db: AsyncSession,
    diary_id: int
) -> None:
    """
    다이어리에 속한 모든 사진의 분석 결과를 집계하여
    DiaryAnalysis 테이블에 저장합니다.

    로직:
    - 모든 사진의 restaurant_name_candidates를 합침
    - 빈도수로 정렬 및 중복 제거
    - category, menu도 동일하게 처리
    """
    pass
```

**구현 포인트:**

- 트랜잭션 관리: `async with db.begin()`
- eager loading으로 N+1 쿼리 방지: `selectinload`, `joinedload`
- 소유자 확인: user_id 검증

---

### 3.2 Photo CRUD 서비스

**파일:** `app/services/photo_service.py`

**작성할 함수:**

```python
from fastapi import UploadFile

async def batch_upload_photos(
    db: AsyncSession,
    user_id: UUID,
    target_date: date,
    files: list[UploadFile]
) -> list[PhotoUploadResult]:
    """
    여러 사진을 한 번에 업로드하고 처리합니다.

    처리 순서:
    1. 각 파일에서 EXIF 파싱 (taken_at, GPS)
    2. 시간대 분류 (time_type 결정)
    3. Diary upsert (user_id + date + time_type)
    4. 파일 저장 (로컬 또는 S3)
    5. Photo 레코드 생성
    6. 분석 작업 큐에 추가 (비동기)
    7. 결과 반환
    """
    results = []

    for file in files:
        # 1. EXIF 파싱
        exif_data = extract_exif_data(file.file)

        # 2. 시간대 분류
        time_type = classify_time_type(exif_data["taken_at"])

        # 3. Diary upsert
        diary = await get_or_create_diary(
            db, user_id, target_date, time_type
        )

        # 4. 파일 저장
        image_url = await save_uploaded_file(file)

        # 5. Photo 생성
        photo = Photo(
            diary_id=diary.id,
            image_url=image_url,
            taken_at=exif_data["taken_at"],
            taken_location=format_gps(exif_data),
        )
        db.add(photo)
        await db.commit()
        await db.refresh(photo)

        # 6. 분석 작업 큐 추가 (TODO: Phase 5)
        # await enqueue_analysis_task(photo.id)

        results.append(PhotoUploadResult(
            photo_id=photo.id,
            diary_id=diary.id,
            time_type=time_type,
        ))

    return results

async def get_photos_by_diary(
    db: AsyncSession,
    diary_id: int
) -> list[Photo]:
    """다이어리에 속한 모든 사진 조회"""
    pass

async def delete_photo(
    db: AsyncSession,
    photo_id: int,
    user_id: UUID
) -> None:
    """사진 삭제 (소유자 확인)"""
    pass
```

**구현 포인트:**

- 트랜잭션 범위: 각 파일 처리를 독립적으로 commit
- 실패 시 rollback 및 에러 로깅
- 대용량 파일 처리: 스트리밍 업로드 고려

---

### 3.3 분석 서비스 (스텁)

**파일:** `app/services/analysis_service.py`

**작성할 함수:**

```python
async def analyze_photo(
    db: AsyncSession,
    photo_id: int
) -> PhotoAnalysisResult:
    """
    사진 하나를 분석합니다.

    처리 순서:
    1. Photo 조회
    2. 이미지 다운로드 (URL에서)
    3. LLM API 호출 (음식 분석)
    4. GPS → Kakao Map API (식당 검색)
    5. PhotoAnalysisResult 생성
    6. DiaryAnalysis 집계 트리거
    """
    # Phase 5에서 구체적으로 구현
    pass

async def create_stub_analysis_result(
    db: AsyncSession,
    photo_id: int
) -> None:
    """
    Phase 3~4 테스트용: 더미 분석 결과 생성
    """
    result = PhotoAnalysisResult(
        photo_id=photo_id,
        food_category="한식",
        restaurant_name_candidates=[
            {"name": "테스트 식당", "confidence": 0.9}
        ],
        menu_candidates=[
            {"name": "김치찌개", "price": 8000}
        ],
        keywords=["얼큰한", "김치"],
        raw_response="stub response"
    )
    db.add(result)
    await db.commit()
```

**구현 포인트:**

- Phase 3~4에서는 스텁 사용
- Phase 5에서 실제 외부 API 연동

---

## 🔄 Phase 4: API 라우터 구현

> FastAPI 엔드포인트 작성

### 4.1 Diary 라우터

**파일:** `app/routers/diaries.py`

**작성할 엔드포인트:**

```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_session
from app.core.dependencies import get_current_user
from app.models import User

router = APIRouter(prefix="/diaries", tags=["diaries"])

@router.get("/{target_date}", response_model=DiariesByDateResponse)
async def get_diaries_by_date(
    target_date: date,
    db: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user)
):
    """
    특정 날짜의 모든 다이어리 조회

    Response:
    {
        "date": "2026-01-19",
        "diaries": [...]
    }
    """
    diaries = await get_diaries_by_date(db, current_user.id, target_date)

    # DiariesByDateResponse로 변환
    diary_responses = []
    for diary in diaries:
        photos = [
            PhotoInDiary(
                photo_id=p.id,
                image_url=p.image_url,
                analysis_status=get_analysis_status(p)
            )
            for p in diary.photos
        ]

        diary_responses.append(DiaryWithPhotos(
            **diary.__dict__,
            photos=photos,
            analysis_status=get_diary_analysis_status(diary),
            cover_photo_url=diary.cover_photo.image_url if diary.cover_photo else None
        ))

    return DiariesByDateResponse(
        date=target_date,
        diaries=diary_responses
    )

@router.get("/{diary_id}/analysis", response_model=DiaryAnalysisResponse)
async def get_diary_analysis_candidates(
    diary_id: int,
    db: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user)
):
    """
    다이어리의 AI 분석 후보 조회

    "이 식당 맞나요?" 화면에서 사용
    """
    analysis = await get_diary_analysis(db, diary_id, current_user.id)

    if not analysis:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="Analysis not found"
        )

    return DiaryAnalysisResponse(
        restaurant_candidates=analysis.restaurant_candidates,
        category_candidates=analysis.category_candidates,
        menu_candidates=analysis.menu_candidates
    )

@router.post("/{diary_id}/confirm", response_model=DiaryResponse)
async def confirm_diary_info(
    diary_id: int,
    confirm_data: DiaryConfirm,
    db: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user)
):
    """
    유저가 AI 추정 결과를 확정합니다.

    - 최초 확정
    - 재수정
    - 덮어쓰기

    모두 이 엔드포인트에서 처리
    """
    diary = await confirm_diary(db, diary_id, current_user.id, confirm_data)

    return DiaryResponse.model_validate(diary)
```

---

### 4.2 Photo 라우터

**파일:** `app/routers/photos.py`

**작성할 엔드포인트:**

```python
from fastapi import APIRouter, Depends, File, UploadFile, Form
from typing import Annotated

router = APIRouter(prefix="/photos", tags=["photos"])

@router.post("/batch-upload", response_model=BatchUploadResponse)
async def batch_upload_photos(
    date: Annotated[str, Form()],  # "2026-01-19"
    photos: Annotated[list[UploadFile], File()],
    db: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user)
):
    """
    여러 사진을 한 번에 업로드합니다.

    Request:
    - Content-Type: multipart/form-data
    - date: string (YYYY-MM-DD)
    - photos[]: file[]

    Response:
    {
        "created": [
            {"photo_id": 101, "diary_id": 12, "time_type": "lunch"},
            ...
        ]
    }
    """
    # date 문자열을 date 객체로 변환
    target_date = datetime.strptime(date, "%Y-%m-%d").date()

    # 파일 형식 검증
    for photo in photos:
        if not photo.content_type.startswith("image/"):
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=f"Invalid file type: {photo.content_type}"
            )

    # 배치 업로드 처리
    results = await batch_upload_photos(
        db, current_user.id, target_date, photos
    )

    return BatchUploadResponse(created=results)

@router.delete("/{photo_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_photo_endpoint(
    photo_id: int,
    db: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user)
):
    """사진 삭제"""
    await delete_photo(db, photo_id, current_user.id)
    return None
```

**구현 포인트:**

- `multipart/form-data` 처리
- 파일 크기 제한: `File(... max_length=10*1024*1024)` (10MB)
- 파일 타입 검증: MIME type 확인
- 대용량 처리: 비동기 처리 및 청크 업로드

---

### 4.3 라우터 등록

**파일:** `app/main.py`

**수정할 내용:**

```python
from app.routers import auth, health, diaries, photos

# 기존 라우터들
app.include_router(health.router)
app.include_router(auth.router)

# 새로운 라우터 추가
app.include_router(diaries.router)
app.include_router(photos.router)
```

---

## 🔄 Phase 5: 외부 API 연동

> LLM, Kakao Map 등 외부 서비스 연동

### 5.1 Kakao Map API 연동

**파일:** `app/services/kakao_map_service.py`

**작성할 함수:**

```python
import httpx
from app.core.config import settings

async def search_nearby_restaurants(
    latitude: float,
    longitude: float,
    radius: int = 500
) -> list[dict]:
    """
    GPS 좌표 주변의 음식점을 검색합니다.

    Returns:
        [
            {
                "name": "식당명",
                "address": "주소",
                "category": "한식",
                "distance": 150
            },
            ...
        ]
    """
    url = "https://dapi.kakao.com/v2/local/search/keyword.json"
    headers = {
        "Authorization": f"KakaoAK {settings.KAKAO_REST_API_KEY}"
    }
    params = {
        "query": "음식점",
        "x": longitude,
        "y": latitude,
        "radius": radius,
        "category_group_code": "FD6"  # 음식점 카테고리
    }

    async with httpx.AsyncClient() as client:
        response = await client.get(url, headers=headers, params=params)
        response.raise_for_status()

        data = response.json()
        places = data.get("documents", [])

        return [
            {
                "name": place["place_name"],
                "address": place["address_name"],
                "category": place["category_name"],
                "distance": place["distance"]
            }
            for place in places
        ]
```

**필요한 설정:**

- `.env`에 `KAKAO_REST_API_KEY` 추가
- `app/core/config.py`에 설정 추가

---

### 5.2 LLM API 연동 (Gemini)

**파일:** `app/services/llm_service.py`

**작성할 함수:**

```python
import google.generativeai as genai
from app.core.config import settings

genai.configure(api_key=settings.GEMINI_API_KEY)

async def analyze_food_image(image_path: str) -> dict:
    """
    이미지를 분석하여 음식 정보를 추출합니다.

    Returns:
        {
            "food_category": "한식",
            "menus": ["김치찌개", "된장찌개"],
            "keywords": ["얼큰한", "구수한"],
            "description": "..."
        }
    """
    model = genai.GenerativeModel("gemini-1.5-flash")

    prompt = """
    이 음식 사진을 분석해주세요.

    다음 정보를 JSON 형식으로 반환해주세요:
    - food_category: 음식 카테고리 (한식, 중식, 일식, 양식 등)
    - menus: 추정되는 메뉴 이름 리스트
    - keywords: 음식을 설명하는 키워드 3~5개
    - description: 음식에 대한 간단한 설명
    """

    # 이미지 로드
    with open(image_path, "rb") as f:
        image_data = f.read()

    response = await model.generate_content_async([prompt, image_data])

    # JSON 파싱
    import json
    result = json.loads(response.text)

    return result
```

**필요한 패키지:**

- `google-generativeai`를 requirements.txt에 추가

---

### 5.3 분석 서비스 완성

**파일:** `app/services/analysis_service.py`

**수정할 함수:**

```python
async def analyze_photo(
    db: AsyncSession,
    photo_id: int
) -> PhotoAnalysisResult:
    """사진 분석 완전 구현"""

    # 1. Photo 조회
    photo = await db.get(Photo, photo_id)
    if not photo:
        raise ValueError("Photo not found")

    # 2. LLM 분석
    llm_result = await analyze_food_image(photo.image_url)

    # 3. GPS가 있으면 Kakao Map 검색
    restaurant_candidates = []
    if photo.taken_location:
        lat, lng = parse_gps_string(photo.taken_location)
        nearby = await search_nearby_restaurants(lat, lng)
        restaurant_candidates = [
            {"name": r["name"], "confidence": 0.8, "address": r["address"]}
            for r in nearby[:5]  # 상위 5개
        ]

    # 4. PhotoAnalysisResult 생성
    result = PhotoAnalysisResult(
        photo_id=photo_id,
        food_category=llm_result["food_category"],
        restaurant_name_candidates=restaurant_candidates,
        menu_candidates=[
            {"name": menu} for menu in llm_result["menus"]
        ],
        keywords=llm_result["keywords"],
        raw_response=str(llm_result)
    )
    db.add(result)
    await db.commit()

    # 5. DiaryAnalysis 집계
    await aggregate_photo_analysis_to_diary(db, photo.diary_id)

    return result
```

---

### 5.4 백그라운드 작업 (선택사항)

**파일:** `app/services/background_tasks.py`

**작성할 함수:**

```python
from fastapi import BackgroundTasks

async def enqueue_photo_analysis(
    background_tasks: BackgroundTasks,
    db: AsyncSession,
    photo_id: int
):
    """사진 분석 작업을 백그라운드에 추가"""
    background_tasks.add_task(analyze_photo, db, photo_id)
```

**라우터에서 사용:**

```python
@router.post("/batch-upload")
async def batch_upload_photos(
    ...,
    background_tasks: BackgroundTasks
):
    # ...
    for result in results:
        await enqueue_photo_analysis(
            background_tasks, db, result.photo_id
        )
```

---

## 🔄 Phase 6: 테스트 작성

> 단위 테스트 및 통합 테스트

### 6.1 서비스 레이어 테스트

**파일:** `tests/test_diary_service.py`

**작성할 테스트:**

```python
import pytest
from datetime import date
from uuid import uuid4

@pytest.mark.asyncio
async def test_get_or_create_diary(db_session, test_user):
    """다이어리 생성 및 조회 테스트"""
    # 첫 번째 호출: 생성
    diary1 = await get_or_create_diary(
        db_session,
        test_user.id,
        date(2026, 1, 29),
        "lunch"
    )
    assert diary1.id is not None

    # 두 번째 호출: 기존 것 반환
    diary2 = await get_or_create_diary(
        db_session,
        test_user.id,
        date(2026, 1, 29),
        "lunch"
    )
    assert diary1.id == diary2.id

@pytest.mark.asyncio
async def test_confirm_diary(db_session, test_user, test_diary):
    """다이어리 확정 테스트"""
    confirm_data = DiaryConfirm(
        restaurant_name="명동교자",
        category="한식"
    )

    updated = await confirm_diary(
        db_session,
        test_diary.id,
        test_user.id,
        confirm_data
    )

    assert updated.restaurant_name == "명동교자"
    assert updated.category == "한식"
```

---

### 6.2 API 엔드포인트 테스트

**파일:** `tests/test_diaries_api.py`

**작성할 테스트:**

```python
import pytest
from httpx import AsyncClient

@pytest.mark.asyncio
async def test_get_diaries_by_date(client: AsyncClient, auth_headers):
    """날짜별 다이어리 조회 API 테스트"""
    response = await client.get(
        "/diaries/2026-01-29",
        headers=auth_headers
    )

    assert response.status_code == 200
    data = response.json()
    assert data["date"] == "2026-01-29"
    assert isinstance(data["diaries"], list)

@pytest.mark.asyncio
async def test_batch_upload_photos(client: AsyncClient, auth_headers):
    """사진 배치 업로드 API 테스트"""
    files = [
        ("photos", ("test1.jpg", open("tests/fixtures/test1.jpg", "rb"), "image/jpeg")),
        ("photos", ("test2.jpg", open("tests/fixtures/test2.jpg", "rb"), "image/jpeg"))
    ]

    response = await client.post(
        "/photos/batch-upload",
        headers=auth_headers,
        data={"date": "2026-01-29"},
        files=files
    )

    assert response.status_code == 200
    data = response.json()
    assert len(data["created"]) == 2
```

---

## 📝 필요한 패키지 추가

**파일:** `requirements.txt`

**추가할 패키지:**

```txt
# 이미지 처리
Pillow==10.2.0

# Google Generative AI
google-generativeai==0.3.2

# HTTP 클라이언트 (이미 있음)
httpx==0.28.1
```

---

## 🎯 우선순위 및 작업 순서

### 권장 작업 순서

1. **Phase 1**: Pydantic 스키마 (1~2시간)
2. **Phase 2**: 유틸리티 함수 (2~3시간)
3. **Phase 3**: 서비스 레이어 (4~6시간)
   - 먼저 스텁 분석 사용
4. **Phase 4**: API 라우터 (3~4시간)
   - Postman/Swagger로 테스트
5. **Phase 5**: 외부 API 연동 (3~4시간)
   - LLM 및 Kakao Map
6. **Phase 6**: 테스트 작성 (2~3시간)

**총 예상 시간: 15~22시간**

---

## ✅ 체크리스트

각 Phase를 완료하면 체크:

- [x] Phase 1: Pydantic 스키마 정의 ✅
  - [x] `app/schemas/diary.py`
  - [x] `app/schemas/photo.py`
  - [x] `app/schemas/auth.py` 확인 (이미 완료)

- [x] Phase 2: 유틸리티 함수 ✅
  - [x] `app/utils/exif_parser.py`
  - [x] `app/utils/file_storage.py`
  - [x] `app/utils/status_helper.py`
  - [x] `requirements.txt`에 Pillow 추가

- [ ] Phase 3: 서비스 레이어
  - [ ] `app/services/diary_service.py`
  - [ ] `app/services/photo_service.py`
  - [ ] `app/services/analysis_service.py` (스텁)

- [ ] Phase 4: API 라우터
  - [ ] `app/routers/diaries.py`
  - [ ] `app/routers/photos.py`
  - [ ] `app/main.py` 라우터 등록

- [ ] Phase 5: 외부 API 연동
  - [ ] `app/services/kakao_map_service.py`
  - [ ] `app/services/llm_service.py`
  - [ ] `app/services/analysis_service.py` 완성

- [ ] Phase 6: 테스트
  - [ ] `tests/test_diary_service.py`
  - [ ] `tests/test_photo_service.py`
  - [ ] `tests/test_diaries_api.py`
  - [ ] `tests/test_photos_api.py`

---

## 📌 참고사항

### 로컬 개발 시

1. Docker로 PostgreSQL 실행
2. 가상환경 활성화
3. uvicorn 실행: `uvicorn app.main:app --reload`
4. Swagger: http://localhost:8000/docs

### 환경변수 필수 설정

```env
DATABASE_URL=postgresql://...
JWT_SECRET_KEY=...
GOOGLE_CLIENT_ID=...
KAKAO_REST_API_KEY=...
GEMINI_API_KEY=...
```

### 디버깅 팁

- SQLAlchemy 쿼리 로그: `DEBUG=true` 설정
- Pydantic 검증 오류: `response.json()` 확인
- 파일 업로드 오류: Content-Type 및 파일 크기 확인

---

## 🚀 시작하기

LLM에게 다음과 같이 요청하세요:

```
"Phase 1부터 시작해줘. app/schemas/diary.py 파일을 작성해줘."
```

각 Phase를 순차적으로 진행하며, 이전 Phase가 완료되어야 다음으로 넘어갑니다.
